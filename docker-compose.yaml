services:
    auth-service:
        container_name: auth-service
        restart: always
        build:
            context: .
            dockerfile: Dockerfile
        env_file:
            - .env.production.local
        # ports:
        #   - "9101:9101"
        expose:
           - "9101"
        networks:
            - kirt08

networks:
    kirt08:
        external: true